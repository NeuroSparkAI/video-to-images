{
    "version": 2,
    "build": {
        "maxCacheSizeMb": 500,
        "timeout": 900,  # Increase build timeout to 15 minutes
        "builder": "dockerfile"
    },
    "deploy": {
        "restartPolicy": "always",
        "timeout": 600
    }
}
